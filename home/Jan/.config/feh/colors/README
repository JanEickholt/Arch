Usage:
IN BASH!
git clone git://git.2f39.org/colors
makefile

colors -n 64 < wallpaper.png | ./bin/hex2html > output.html


What is it?
===========

A simple program to extract colors from PNG files.  It is similar to
strings(1) but for pictures.  For a given input file it outputs a
colormap to stdout.

    # ./colors -p < input.png | ./bin/hex2html > output.html
